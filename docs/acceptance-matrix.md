# V1 Acceptance Matrix

This matrix is the execution checklist for `docs/product-v1.md`. A row is accepted only when the evidence column contains a repeatable test, command output, or manual evidence artifact. A passing unit test that does not exercise the stated boundary is insufficient.

| ID | Area | Acceptance statement | Planned evidence | Current status |
|---|---|---|---|---|
| AC-001 | Scope | V1 is documented as single-tenant demand forecasting and replenishment assistance with human confirmation | `docs/product-v1.md` review | Passed: contract section 1-2 |
| AC-002 | Scope | Automatic ordering, ERP/WMS sync, Agent/RAG, microservices, and multi-tenant roles are explicitly out of scope | Product contract review | Passed: contract section 6 |
| AC-003 | Data | Valid sales CSV passes schema, finite-value, positive-ID, and unique-key validation | `tests/test_dataset_import.py` | Passed: validator and valid import tests |
| AC-004 | Data | Invalid sales input is rejected without changing the active dataset | Import integration test with active-version sentinel | Passed: previous pointer remains unchanged |
| AC-005 | Data | Valid inventory snapshot exposes stock, inbound, reserved, lead-time, policy, and freshness fields | `tests/test_inventory_import.py` | Passed: immutable import/active pointer, metadata API, freshness age, and inventory cell policy fields are covered |
| AC-006 | Data | Sparse product/store combinations are represented as absent or unavailable, never fabricated by a cross product | Dataset and forecast service test | Passed: import manifest preserves source row count |
| AC-007 | Metrics | Quantity is labeled as quantity; monetary value is only shown after quantity-times-price calculation with currency | API schema and frontend contract test | Passed: quantity labels and `unit: "units"` |
| AC-008 | Metrics | Historical 30-day and forecast 30-day windows expose explicit start/end dates and as-of date | API response test | Passed: KPI window contract and regression test |
| AC-009 | Metrics | Top products, category totals, and KPI use one documented time window or clearly state different windows | Aggregation fixtures and UI review | Passed: dashboard uses the explicit `HISTORICAL_DAYS = 30` constant for KPI history, Top products, category totals, and ABC demand; regression coverage verifies Top/category aggregation calls receive 30 days |
| AC-010 | ABC | ABC population is fixed before scope filtering, with deterministic ties and threshold-boundary tests | `tests/test_abc.py`, dashboard fixture | Passed: single-item, dominant-item, tie, zero, and scoped-population tests |
| AC-011 | ABC | ABC is presented as demand priority and is not used alone as stockout risk | Schema/UI copy test | Passed: ABC remains a demand-priority field, while inventory risk and suggested quantity now require a valid active inventory snapshot; the former ABC-only fallback was removed and missing inventory returns `503 INVENTORY_UNAVAILABLE` |
| AC-012 | Forecast | A product/store pair with insufficient history returns unavailable, not zero demand | Forecast API test | Passed: insufficient-history `ValueError` maps to `503 FORECAST_UNAVAILABLE` |
| AC-013 | Forecast | Forecast recursion updates all required future features from origin-known or previously predicted values | `tests/test_future_features.py` plus real predictor smoke | Passed: shared helper is used by LSTM/LightGBM training adapters and serving recursion; real retraining published the matching feature schema and a 30-point forecast |
| AC-014 | Forecast | Backtest uses identical origins, horizons, scopes, and eligible sample keys for models and baselines | `tests/test_backtest.py` plus generated model report | Passed: leakage-safe LSTM/LightGBM adapters and seasonal-naive baseline use 3 origins, 30 steps, 300 eligible keys, 9,000 samples each; model and baseline key sets match |
| AC-015 | Forecast | Report includes horizon, origin count, sample count, per-horizon metrics, and segment metrics | Model report contract test plus generated report | Passed: report contains 30-day horizon, 3 origins, 300 keys, 9,000 samples, 30 per-horizon buckets, and 100 product/store segments for each model and baseline |
| AC-016 | Forecast | Model complexity does not override a stronger baseline | Model selection test and validation/test report review | Passed: validation compares LSTM, LightGBM, seasonal-naive, and four candidate ensemble weights by WAPE; the generated package selected LightGBM and exposes the selection metadata |
| AC-017 | Intervals | Statistical intervals include target coverage, empirical coverage, calibration window, and sample size | Prediction interval report test | Removed from V1 by `docs/product-v1.md` section 3.3: statistical calibration is explicitly out of scope; V1 uses the documented `情景范围` path and AC-018 is the active interval requirement |
| AC-018 | Intervals | If interval calibration is insufficient, UI uses scenario-range wording or hides the interval | Frontend copy test and manual screenshot | Passed: API `range_type=scenario`, UI label, and non-statistical copy are covered |
| AC-019 | Replenishment | Suggested quantity follows net available, lead time, review period, safety stock, pack size, and MOQ | `tests/test_replenishment.py` hand-calculated fixtures | Passed: hand-calculated and active-snapshot tests pass; real browser tooltip exposes window demand, net available, target stock, raw replenishment, pack size, and MOQ; saved CSV preserves the same policy/version fields |
| AC-020 | Replenishment | Missing or stale inventory inputs block a suggestion and identify the missing input | Domain/API test | Passed for endpoint contract: import validation, missing-field validation, stale snapshot 503, and missing product/store snapshot key 503 include the relevant input/key |
| AC-021 | Replenishment | Lead-time plus review window beyond forecast horizon is rejected | Boundary test | Passed: domain rule rejects a window beyond the forecast horizon |
| AC-022 | Plans | A replenishment draft can be saved, reopened after restart, and exported with immutable version metadata | `tests/test_plans.py` and restore run | Passed: real browser save returned `201`, export returned `200` and downloaded CSV; a fresh backend process reopened the same `plan_id` with 100 items, and saved/exported rows retained data/model/inventory/policy versions |
| AC-023 | Plans | Repeating an idempotency key does not create duplicate drafts | API integration test | Passed: same key returns the same `plan_id`; conflicting payload returns 409 |
| AC-024 | Failure semantics | Negative input is `422`, unknown resource is `404`, unavailable dependency is `503`, and all failed forecasts are not represented as zero | API error tests | Passed: forecast/dashboard regression tests |
| AC-025 | Failure semantics | Partial prediction includes requested/succeeded/failed counts and prevents saving an incomplete draft | Service/API/UI tests | Passed: coverage/UI warning, disabled save state, API save gate, and injected browser partial-failure run (`成功 1/2，失败 1 项`) pass |
| AC-026 | Readiness | Liveness can be healthy while readiness fails for missing, corrupt, or incompatible data/model artifacts | Health tests and container probe | Passed for local Docker path: health/readiness/liveness tests cover artifact failures, and the rebuilt Compose backend is healthy with container probes active |
| AC-027 | Versioning | A dataset/model activation is atomic; interrupted activation leaves the previous active version usable | Artifact lifecycle integration test | Passed: atomic pointer interruption, temporary-file cleanup, failed publish preservation, and fresh-process active-package resolution are covered |
| AC-028 | Versioning | Cache keys and saved results include data/model/policy versions | Cache and plan snapshot test | Passed for saved plans: snapshot and CSV include data/model/inventory/policy versions; forecast cache includes data/model versions |
| AC-029 | Security | Configured API authentication protects real routers, not only an unused helper | Auth integration tests | Passed: missing token 401 and correct token 200 on `/api/products` |
| AC-030 | Security | Rate limiting has a real enforcement point and returns a documented response when exceeded | Repeated-request integration test | Passed: configured zero limit returns 429 with `Retry-After` |
| AC-031 | Security | No real env file, token, logs, test data, or model artifacts enter the production image | Image content inspection | Passed: rebuilt backend image inspection excludes `.env`, database backup, runtime data, inventory snapshot, saved models, logs, tests, and pytest cache; frontend image contains only static assets and the required license notice |
| AC-032 | Security | Untrusted model artifacts are rejected; dependencies are scanned and applicable high-severity findings are handled | Artifact negative test plus dependency report | Passed for the current pinned set: artifact protections pass; FastAPI/Starlette/LightGBM/pip/setuptools were upgraded; `torch==2.14.0+cpu` is resolved from the PyTorch CPU index and the strict OSV audit reports no known vulnerabilities |
| AC-033 | Security | Product/store text is rendered as text or safely escaped in tooltips and exports | XSS fixture test | Passed: browser XSS fixture kept `window.__xss=0`, created no injected `img`/`svg` elements, and preserved tooltip text; shared escaping, export, and static contract tests also pass |
| AC-034 | Frontend | Fast scope changes cannot let an older response overwrite a newer scope | Delayed-request browser test | Passed: delayed browser fixture ended at `#2 P2 · 全部门店` with `scope-2` timestamp and no error; dashboard/trend/heatmap request IDs and cancellation guards are covered |
| AC-035 | Frontend | Timeout, CDN failure, API failure, empty data, partial data, stale data, and retry have visible states | Browser E2E test and screenshots | Passed for the local browser path: bundled ECharts removes runtime CDN dependency; timeout, retry, API 500, empty, and partial fixtures produced visible settled states, while metadata freshness drives the stale/warning state |
| AC-036 | Frontend | Trend chart handles empty history and draws the intended lower/upper interval band | Chart unit/browser test | Passed: empty fixture shows the empty state; the real ECharts instance renders 120-point `情景范围下界` plus 120-point positive-width `情景范围` band with `stack=scenario-range` |
| AC-037 | Accessibility | Core selection, refresh, save, and export flow is keyboard reachable and labeled | Browser keyboard test | Passed: real Tab traversal reached scope selectors, refresh, plan name, save, trend selectors, and chart aria nodes; Enter on the focused save button created a plan and Enter on the focused export link downloaded CSV |
| AC-038 | Responsive | 1920x1080, 1366x768, 390x844, and 200% zoom preserve access to core actions | Browser screenshots and checklist | Partial: real screenshots pass at 1920x1080, 1366x768, and 390x844; CSS/browser zoom smoke evidence keeps refresh/save visible, but the exact repeatable 200% target-browser run remains |
| AC-039 | Performance | On a fixed 4-core/8GB reference environment, warm read API P95 <=500ms and cold full dashboard <=10s | Benchmark script output | Passed: backend cgroups were constrained to 4 CPUs/8 GiB; 100 warm `/api/model-info` requests at concurrency 10 had P95 58.61ms, cold dashboard was 235.92ms, and the 300-second run had P95 247.88ms |
| AC-040 | Performance | Ten concurrent users for five minutes keep error rate below 1% without unbounded memory growth | Load test report | Passed: under the same 4 CPU/8 GiB constraint, 1,500 dashboard requests over 300 seconds at concurrency 10 had 0 failures; memory ranged 327.6-364.8MB and ended at 331.0MB. Default rate limiting separately returned the expected 429 responses |
| AC-041 | Deployment | Clean clone can install, validate, build, start, and verify without hidden local artifacts | Deployment transcript | Passed for local Docker path: `docker compose build --no-cache`, Compose start, healthy backend/frontend probes, all deployment endpoints, 404 error path, and image-content inspection pass; Windows used equivalent PowerShell endpoint checks because no native `/bin/bash` is installed |
| AC-042 | Deployment | Readiness failure and backend outage make deployment verification exit non-zero | Fault-injected deployment test | Passed for local Compose path: stopping backend made `/health` refuse the connection and frontend `/api/products` return `502`; restarting returned the backend to healthy and `/health=200` |
| AC-043 | Recovery | Dataset/model rollback and replenishment-draft backup/restore are each exercised once | Recovery runbook evidence | Passed for local V1 path: `docs/recovery-runbook.md` documents dataset/model activation and SQLite restore commands; CLI walkthrough and 14 focused tests cover manifest/integrity validation, atomic replacement, and previous-version safety; Docker stop/start recovery is recorded in `docs/deployment-performance-2026-09-15.md` |
| AC-044 | Tests | Core domain/data branches reach at least 85% coverage and critical failure paths are present | Coverage report plus test list | Passed: 160 tests, 90.66% measured coverage, Ruff and failure-path tests pass |
| AC-045 | Docs | A non-author developer follows README to import, forecast, save, export, and recover a draft | Fresh-environment walkthrough | Passed: `docs/fresh-clone-walkthrough-2026-09-15.md` records a remote fresh clone using Python 3.11 and pinned dependencies; initialization, sales import, model training, 160 tests, health/readiness, 30-point forecast, save/idempotency/export, restart reopen, and SQLite restore all completed |
| AC-046 | Release | No P0/P1 remains open; all exceptions have owner, rationale, and target release | Release sign-off in `docs/releases/` | Partial: `docs/releases/v1-acceptance.md` records the verified baseline, owners, rationales, target release, and evidence required; the decision remains `NOT READY FOR RELEASE` until the listed P1/deployment gaps are closed |

## Current Baseline Evidence

The following are facts from the audit before TASK-001 implementation:

- 82 backend tests passed in 10.33 seconds.
- Ruff, JavaScript syntax checks, Compose config validation, and `pip check` passed.
- Remote CI result has not yet been recorded; current local coverage, browser, load, image-content, and dependency evidence is recorded in the linked evidence documents.
- Read-only fault injection reproduced: configured token without a request header returned 200; missing assets returned HTTP 200 with `degraded`; negative forecast ID returned 500; all forecasts failing returned a normal dashboard with predicted total 0.

These baseline results are retained as evidence for the work items that must change them. They are not acceptance of the corresponding V1 rows.

## Release Gate

The release gate is closed until every `AC-001` through `AC-046` is either evidenced as passed or explicitly removed from V1 by a reviewed update to `docs/product-v1.md`. A status of `Planned`, `Not started`, or `Cannot confirm` is not a pass.
